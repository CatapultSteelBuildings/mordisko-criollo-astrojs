---
import Section from '@components/shared/Section.astro';

import { getLangFromUrl } from '@i18n/utils';

import WhatsappHomeSection from './WhatsappHome.section.astro';
import ContactLogo from '@components/home/ContactLogo.astro';
import Delivery from '@components/shared/Delivery.astro';

const lang = getLangFromUrl(Astro.url);

const title = lang === 'es' ? '¿Te antojaste de algo?' : 'Got a craving?';
---

<Section variant="secondary" title={title}>
  <div class="ContactHomeSection">
    <WhatsappHomeSection />
    <ContactLogo />
    <Delivery variant="secondary" classes="lg:col-span-2 lg:flex-row" />
  </div>
</Section>

<style>
  @reference "tailwindcss";
  .ContactHomeSection {
    @apply mx-auto max-w-[1200px] lg:grid lg:grid-cols-2 lg:gap-x-3;
  }
</style>
