---
import Layout from '@layouts/Layout.astro';
import HeroProductsSection from '@components/products/hero-products-section.astro';
import ProductDescriptionSection from '@components/products/Product-description-section.astro';
import ProductsMainSection from '@components/products/products-main-section.astro';

interface Props {
  location: string;
}

const { location } = Astro.props;

const title = 'Productos';
const metaDescription = 'Productos';
---

<Layout location={location} title={title} metaDescription={metaDescription}>
  <HeroProductsSection />
  <ProductDescriptionSection />
  <ProductsMainSection />
</Layout>
